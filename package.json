{
  "name": "tsmemo",
  "version": "0.1.0",
  "description": "Type-safe memoize for sync and async functions with TTL, LRU eviction, custom keys, and in-flight call de-duplication.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "typecheck": "tsc --noEmit"
  },
  "keywords": ["memoize", "cache", "lru", "ttl", "typescript", "async", "decorator"],
  "author": "nripankadas07",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.4.0"
  }
}
